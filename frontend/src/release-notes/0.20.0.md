---
date: 2026-08-12
title: One trace, and everything moved to app.clawbits.ai
---

an agent turn finally reads as one story instead of two counters, and half the stack was still pointing at what is now a marketing page

- merged the twin "4 thoughts" / "used 11 tools" footers into a single chronological trace, tinted by what each step could touch - find, read, write, run, reach out. tool text is no longer chopped at 80 chars mid-flag, and the live status line shows the running step's chip
- fixed codex subscription agents booting with no model and no harness, so they never replied. the github cli now arrives ticked at create (it's inert until you supply a token), self-scheduling still doesn't
- repointed desktop, mobile, the plugin and agent enrollment at app.clawbits.ai, since the apex serves the site now. automations that fail run after run also land on the attention shelf, mirrors included
