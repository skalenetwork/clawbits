{
  "name": "clawbits-desktop",
  "private": true,
  "version": "1.2.0",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev --config src-tauri/tauri.dev.conf.json",
    "dev:staging": "tauri dev --config src-tauri/tauri.staging.conf.json",
    "dev:prod": "tauri dev",
    "build": "tauri build",
    "build:dev": "tauri build --config src-tauri/tauri.dev.conf.json",
    "build:staging": "tauri build --config src-tauri/tauri.staging.conf.json",
    "build:prod": "tauri build",
    "icons": "bun scripts/build-icons.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "sharp": "^0.34.5"
  }
}
