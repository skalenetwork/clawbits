{
  "//": "Runs INSIDE the mirror repo skalenetwork/clawbits-openclaw-plugin (see .github/workflows/publish-clawhub-plugin.yaml), so these v* tags live in that repo's namespace, not this one. Conventional Commits; the default preset covers feat/fix/perf/revert and BREAKING CHANGE.",
  "repositoryUrl": "https://github.com/skalenetwork/clawbits-openclaw-plugin.git",
  "tagFormat": "v${version}",
  "branches": [
    "main"
  ],
  "plugins": [
    [
      "@semantic-release/commit-analyzer",
      {
        "releaseRules": [
          {
            "type": "refactor",
            "release": "patch"
          }
        ]
      }
    ],
    "@semantic-release/release-notes-generator",
    [
      "@semantic-release/github",
      {
        "successComment": false,
        "failComment": false,
        "labels": false
      }
    ]
  ]
}
