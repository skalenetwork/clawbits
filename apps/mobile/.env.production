# app.clawbits.ai, NOT the apex - clawbits.ai serves the marketing site, so the
# apex answers every /api/* call with the site's HTML 404 instead of the API.
# This file OVERRIDES the fallback in src/lib/config.ts, so it has to move too.
EXPO_PUBLIC_CLAWBITS_API_URL=https://app.clawbits.ai
